import javax.swing.ImageIcon;

public class Player {
  int x, y, speedx, speedy;
  ImageIcon image;
  public Player(int x, int y, ImageIcon image) {
    this.x = x;
    this.y = y;
    this.image = image;
  }
}
